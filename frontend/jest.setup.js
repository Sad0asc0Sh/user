import "@testing-library/jest-dom/extend-expect";

// Mock Next.js Image to render a basic img
jest.mock("next/image", () => ({
  __esModule: true,
  default: (props) => {
    // eslint-disable-next-line @next/next/no-img-element
    return <img {...props} />;
  },
}));
