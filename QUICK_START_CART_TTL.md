# راهنمای سریع: سیستم مدیریت سبد خرید

## شروع سریع (5 دقیقه)

### 1. تنظیمات اولیه (پنل ادمین)

**مسیر:** پنل ادمین → تنظیمات → تنظیمات سبد خرید

#### پیکربندی استاندارد (توصیه شده):
```
☐ سبدهای خرید ماندگار: خاموش
[1] مدت زمان نگهداری: 1 ساعت
☑ انقضای خودکار: روشن
☐ حذف خودکار: خاموش (توصیه می‌شود)
☑ هشدار قبل از انقضا: روشن
[30] زمان هشدار: 30 دقیقه
```

**ذخیره تغییرات** → تنظیمات اعمال می‌شود

### 2. بررسی عملکرد

#### گام 1: افزودن محصول به سبد (از سایت کاربر)
- وارد سایت شوید
- محصولی را به سبد اضافه کنید
- سبد خرید ایجاد می‌شود با مهلت 1 ساعت

#### گام 2: بررسی در پنل ادمین
**مسیر:** پنل ادمین → سفارشات → سبدهای رها شده

شما خواهید دید:
- لیست سبدهای فعال
- زمان باقی‌مانده تا انقضا
- دکمه‌های عملیاتی (حذف، ارسال یادآوری)

#### گام 3: ارسال دستی هشدار
**مسیر:** پنل ادمین → تنظیمات → تنظیمات سبد خرید

کلیک روی:
```
[ارسال دستی هشدارهای انقضا (همین الان)]
```

پیام موفقیت:
```
✅ هشدار انقضا برای X سبد خرید ارسال شد
```

## سناریوهای رایج

### سناریو 1: سبدهای ماندگار (بدون انقضا)

**استفاده:** سایت‌های B2B، لیست خرید طولانی‌مدت

**تنظیمات:**
```
☑ سبدهای خرید ماندگار: روشن
```

**نتیجه:** سبدها هیچ‌گاه منقضی نمی‌شوند

### سناریو 2: فروشگاه با موجودی محدود

**استفاده:** محصولات فیزیکی با موجودی کم

**تنظیمات:**
```
☐ سبدهای خرید ماندگار: خاموش
[0.5] مدت زمان نگهداری: 30 دقیقه
☑ انقضای خودکار: روشن
☑ هشدار قبل از انقضا: روشن
[15] زمان هشدار: 15 دقیقه
```

**نتیجه:** سبد بعد از 30 دقیقه منقضی می‌شود، با هشدار 15 دقیقه قبل

### سناریو 3: محصولات دیجیتال

**استفاده:** کتاب، نرم‌افزار، دوره‌های آنلاین

**تنظیمات:**
```
☐ سبدهای خرید ماندگار: خاموش
[24] مدت زمان نگهداری: 24 ساعت (1 روز)
☑ انقضای خودکار: روشن
☐ هشدار قبل از انقضا: خاموش
```

**نتیجه:** سبد بعد از 24 ساعت منقضی می‌شود، بدون هشدار

## دستورات مدیریتی

### حذف سبد خاص
**مسیر:** پنل ادمین → سفارشات → سبدهای رها شده

1. پیدا کردن سبد مورد نظر
2. کلیک روی دکمه "حذف" 🗑️
3. تایید در دیالوگ
4. سبد به طور کامل حذف می‌شود

### پاکسازی دسته‌جمعی
**مسیر:** پنل ادمین → سفارشات → سبدهای رها شده

کلیک روی:
```
[پاکسازی منقضی‌ها] (دکمه قرمز)
```

**عملکرد:**
- همه سبدهای منقضی شده پیدا می‌شوند
- آیتم‌ها و قیمت‌ها پاک می‌شوند
- سبد به عنوان منقضی علامت‌گذاری می‌شود

### ارسال یادآوری به یک کاربر
**مسیر:** پنل ادمین → سفارشات → سبدهای رها شده

در ستون "عملیات":
```
[ایمیل] → ارسال یادآوری ایمیلی
[پیامک] → ارسال یادآوری پیامکی
```

## API برای توسعه‌دهندگان

### فراخوانی از کد

```javascript
// ارسال هشدارها
await axios.post('/api/carts/admin/send-warnings', {}, {
  headers: { Authorization: `Bearer ${adminToken}` }
})

// پاکسازی سبدها
await axios.post('/api/carts/admin/cleanup', {}, {
  headers: { Authorization: `Bearer ${adminToken}` }
})

// حذف سبد خاص
await axios.delete(`/api/carts/admin/${cartId}`, {
  headers: { Authorization: `Bearer ${adminToken}` }
})
```

### تنظیم Cron Job (اختیاری)

**نصب:**
```bash
npm install node-cron
```

**کد (در server.js):**
```javascript
const cron = require('node-cron')

// هر 15 دقیقه: ارسال هشدارها
cron.schedule('*/15 * * * *', async () => {
  await axios.post('http://localhost:5000/api/carts/admin/send-warnings')
})

// هر روز ساعت 3 صبح: پاکسازی
cron.schedule('0 3 * * *', async () => {
  await axios.post('http://localhost:5000/api/carts/admin/cleanup')
})
```

## سوالات متداول

### Q: آیا می‌توانم سبدها را برای همیشه نگه دارم؟
**A:** بله، با فعال کردن "سبدهای خرید ماندگار" در تنظیمات.

### Q: هشدارها به کی ارسال می‌شوند؟
**A:** به ایمیل و شماره تلفن ثبت شده کاربر در پروفایلشان.

### Q: چند بار هشدار ارسال می‌شود؟
**A:** فقط یک بار برای هر سبد، تا زمانی که کاربر سبد را به‌روزرسانی کند.

### Q: اگر کاربر محصولی به سبد اضافه کند، چه می‌شود؟
**A:** زمان انقضا تمدید می‌شود و flag هشدار ریست می‌شود.

### Q: آیا سبدهای حذف شده قابل بازیابی هستند؟
**A:** خیر، حذف دائمی است. برای حفظ داده‌ها، از "پاکسازی" به جای "حذف" استفاده کنید.

### Q: تفاوت "پاکسازی" و "حذف" چیست؟
**A:**
- **پاکسازی**: آیتم‌ها را پاک می‌کند اما سبد را نگه می‌دارد (برای آمار)
- **حذف**: سبد را کاملاً از دیتابیس حذف می‌کند

## مشکلات رایج و راه‌حل

### مشکل: هشدارها ارسال نمی‌شوند

**بررسی 1:** آیا هشدار فعال است?
```
پنل ادمین → تنظیمات → تنظیمات سبد خرید
☑ هشدار قبل از انقضا: روشن
```

**بررسی 2:** آیا کاربران ایمیل و تلفن دارند؟
```
پنل ادمین → مشتریان → لیست مشتریان
بررسی فیلدهای email و phone
```

**بررسی 3:** آیا API Key های SMS و Email تنظیم شده‌اند؟
```
پنل ادمین → تنظیمات → تنظیمات اعلان‌ها
```

### مشکل: سبدها منقضی نمی‌شوند

**بررسی:** آیا انقضای خودکار فعال است؟
```
پنل ادمین → تنظیمات → تنظیمات سبد خرید
☑ انقضای خودکار: روشن
☐ سبدهای خرید ماندگار: خاموش
```

### مشکل: دکمه "ارسال هشدار" نمایش داده نمی‌شود

**دلیل:** یکی از شرایط زیر برقرار است:
- سبدهای ماندگار فعال است
- هشدار قبل از انقضا غیرفعال است

**راه‌حل:**
1. خاموش کردن "سبدهای ماندگار"
2. روشن کردن "هشدار قبل از انقضا"

## چک‌لیست راه‌اندازی

- [ ] تنظیم مدت زمان نگهداری مناسب (پیشنهاد: 1-3 ساعت)
- [ ] فعال‌سازی انقضای خودکار
- [ ] تنظیم هشدار قبل از انقضا (اختیاری)
- [ ] تنظیم API Key های ایمیل و SMS
- [ ] تست ارسال هشدار با یک سبد نمونه
- [ ] بررسی صفحه سبدهای رها شده
- [ ] تنظیم Cron Job برای اتوماسیون (اختیاری)

## منابع بیشتر

- [مستندات کامل](./CART_TTL_ADVANCED_SYSTEM.md)
- [مستندات اولیه](./CART_TTL_SYSTEM.md)

## پشتیبانی

در صورت بروز مشکل:
1. بررسی لاگ‌های سرور
2. بررسی console مرورگر
3. مطالعه مستندات کامل
4. تماس با تیم پشتیبانی
